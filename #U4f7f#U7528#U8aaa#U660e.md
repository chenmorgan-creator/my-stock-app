# 整合投資平台 — 使用說明（純前端版）

這個版本**完全不需要安裝 Python**，也不用跑本地 OCR 後端。所有功能（截圖辨識、Google Sheets 匯入、報價分析、圖表）都在瀏覽器裡完成，換一台電腦只要重新解壓縮、裝一次 Node.js 就能用。

## 你的電腦需要先裝好

**Node.js**（唯一需要安裝的東西）：https://nodejs.org/ 下載「LTS」版本，一路下一步安裝到底即可。

## 怎麼啟動（不用開終端機）

解壓縮這個 zip 之後，資料夾裡有啟動檔，依你的系統點兩下：

| 檔名 | 系統 |
|---|---|
| `啟動.bat` | Windows |
| `啟動.command` | Mac |

雙擊之後會跳出一個黑色視窗（這是正常的，程式就是在裡面跑），幾秒後瀏覽器會自動打開網站。**不要把這個黑色視窗關掉，一關掉網站就會停止**。要關閉網站時，直接把那個黑色視窗關掉即可。

### Mac 使用者請注意

第一次點 `.command` 檔案，Mac 可能會跳出「無法開啟，因為來自未識別的開發者」的警告，這是正常的安全機制，處理方式：
1. 在檔案上按右鍵（或按住 Control 點擊）→ 選擇「打開」
2. 跳出的視窗再按一次「打開」
之後就可以正常雙擊使用了。

如果還是不能執行，打開「終端機」App，輸入以下指令（把路徑換成你解壓縮的資料夾路徑）：
```
chmod +x "啟動.command"
```

## 第一次執行會發生什麼事

- **第一次**點擊啟動檔，程式會自動幫你安裝需要的套件（`npm install`），這個過程可能要 1-2 分鐘，且需要網路連線，請耐心等待。
- 之後**每次啟動只需要幾秒鐘**，因為套件已經裝好了。

## 功能說明

- **📊 投資組合量化分析**：純前端 OCR（Tesseract.js）辨識截圖持股，或手動輸入 / 從 Google Sheets 匯入，接著抓 Yahoo Finance 報價，用瀏覽器原生 SVG 畫排行長條圖，可下載 PNG。
- **📈 美股收益分析系統**：貼上或匯入「日期 收益率」資料，畫出收益曲線圖，可下載 PNG，也支援指定 Google Sheets 分頁（網址帶 `gid=` 參數）。

## 怎麼設定 Google Sheets

點畫面上的「⚙️ 設定 Google Sheets」按鈕，貼上你的試算表分享連結。記得先把該試算表的共用權限改成「知道連結的任何人皆可檢視」，不然會讀取失敗。

## 已知限制（不是你的問題，是設計上的取捨）

- 股價報價是透過公開的 `corsproxy.io` 代理去抓 Yahoo Finance，這個服務不受我們控制，有時會不穩定或抓不到價格，屬於已知限制。
- 純前端 OCR（Tesseract.js）辨識精準度會比本地 Python 版稍低，建議辨識完手動核對、修正表格內容。
- 這個網站目前只能在**你自己的電腦上**執行（`localhost`），還沒有部署到網路上給別人用。
